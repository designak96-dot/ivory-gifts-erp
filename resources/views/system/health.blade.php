@extends('layouts.app') @section('title','System Health') @section('subtitle','Database, storage, backup and integration readiness')
@section('content')<div class="toolbar"><div><a class="btn" href="{{ route('system.import-export') }}">Import / Export</a> @if(auth()->user()->hasPermission('backups.manage'))<a class="btn primary" href="{{ route('backups.index') }}">Backups</a>@endif</div></div><div class="card">@foreach($checks as $name=>$check)<div class="health"><span class="dot {{ $check[0] }}"></span><div><b>{{ $name }}</b><div class="muted">{{ $check[1] }}</div></div></div>@endforeach</div><div class="card" style="margin-top:18px"><h2>Deployment verification</h2><p>From SSH, run <code>php artisan ivory:verify</code>. Every check must show PASS. The deploy script runs this automatically.</p></div>@endsection
