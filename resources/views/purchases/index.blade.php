@extends('layouts.app') @section('title','Purchases') @section('subtitle','Purchase orders, goods receipts and supplier records')
@section('content')<div class="toolbar"><a class="btn" href="{{ route('suppliers.index') }}">Manage suppliers</a></div>@if(auth()->user()->hasPermission('purchases.manage'))<form method="post" action="{{ route('purchases.store') }}" class="card">@csrf<div class="card-header"><h2>New purchase order</h2></div><div class="form-grid"><label>Supplier<select name="supplier_id" required><option value="">Select</option>@foreach($suppliers as $s)<option value="{{ $s->id }}">{{ $s->supplier_code }} · {{ $s->name }}</option>@endforeach</select></label><label>Order date<input type="date" name="order_date" value="{{ today()->toDateString() }}" required></label><label>Expected delivery<input type="date" name="expected_delivery_date"></label><label>Notes<input name="notes"></label></div><div class="table-wrap" style="margin-top:15px"><table><thead><tr><th>Product</th><th>Qty</th><th>Unit cost</th><th>VAT %</th></tr></thead><tbody><tr><td><select name="items[0][product_id]" required><option value="">Select product</option>@foreach($products as $p)<option value="{{ $p->id }}">{{ $p->sku }} · {{ $p->name_en }}</option>@endforeach</select></td><td><input type="number" step=".001" min=".001" name="items[0][qty]" value="1" required></td><td><input type="number" step=".01" min="0" name="items[0][unit_cost]" value="0" required></td><td><input type="number" step=".01" min="0" name="items[0][tax_rate]" value="5"></td></tr></tbody></table></div><div class="actions"><button class="btn primary">Create purchase order</button></div></form>@endif<div class="card" style="margin-top:18px"><h2>Purchase orders</h2><div class="table-wrap" style="margin-top:15px"><table><thead><tr><th>PO</th><th>Supplier</th><th>Date</th><th>Expected</th><th>Status</th><th>Total</th><th></th></tr></thead><tbody>@forelse($orders as $o)<tr><td><b><a href="{{ route('purchases.show',$o) }}">{{ $o->purchase_order_number }}</a></b></td><td>{{ $o->supplier->name }}</td><td>{{ $o->order_date->format('d M Y') }}</td><td>{{ $o->expected_delivery_date?->format('d M Y')??'—' }}</td><td><span class="badge {{ $o->status==='received'?'green':($o->status==='partially_received'?'blue':'amber') }}">{{ str($o->status)->replace('_',' ') }}</span></td><td class="amount">AED {{ number_format($o->grand_total,2) }}</td><td><a class="btn small" href="{{ route('purchases.show',$o) }}">Open</a></td></tr>@empty<tr><td colspan="7" class="empty">No purchase orders.</td></tr>@endforelse</tbody></table></div>{{ $orders->links() }}</div>@endsection
